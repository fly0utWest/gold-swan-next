services:
  gold-swan-web:
    build: .
    environment:
      - NODE_ENV=production
      - GOOGLE_ANALYTICS_ID=${GOOGLE_ANALYTICS_ID}
      - TG_API_URL=${TG_API_URL}
      - GOOGLE_ANALYTICS_ID=${GOOGLE_ANALYTICS_ID}
      - TG_TOKEN=${TG_TOKEN}
      - TG_CHAT_ID=${TG_CHAT_ID}
    ports:
      - "9999:9999"
